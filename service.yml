apiVersion: v1
kind: Service
metadata:
  name: llmops-service
  labels:
    app: llmops
spec:
  type: NodePort
  selector:
    app: llmops
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
    nodePort: 30007
    